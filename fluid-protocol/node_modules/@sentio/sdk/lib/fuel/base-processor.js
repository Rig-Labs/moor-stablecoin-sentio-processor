import { addFuelProcessor, FuelProcessor } from './fuel-processor.js';
// import { FuelCall } from './context.js'
import { FuelChainId } from '@sentio/chain';
export class FuelAbstractProcessor extends FuelProcessor {
    constructor(abi, config) {
        if (!config) {
            config = {
                chainId: FuelChainId.FUEL_MAINNET,
                address: '*'
            };
        }
        super({
            ...config,
            abi
        });
        addFuelProcessor(config, this);
    }
}
export class TypedCall {
    args;
    returnValue;
    argsObject;
    logs;
}
//# sourceMappingURL=base-processor.js.map