import { HandlerType } from '@sentio/protos';
export class SolanaFacet {
    server;
    constructor(server) {
        this.server = server;
    }
    testInstructions(instructions) {
        return this.server.processBindings({
            bindings: instructions.map((instruction) => {
                return {
                    data: {
                        raw: new Uint8Array(),
                        solInstruction: instruction,
                    },
                    handlerIds: [],
                    handlerType: HandlerType.SOL_INSTRUCTION,
                };
            }),
        });
    }
}
//# sourceMappingURL=solana-facet.js.map