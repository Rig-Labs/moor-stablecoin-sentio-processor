export class Cursor {
    _cursor;
    _done = false;
    pageSize;
    constructor(options) {
        this._cursor = options?.initCursor;
        this.pageSize = options?.pageSize;
    }
    get cursor() {
        return this._cursor;
    }
    set cursor(cursor) {
        if (!cursor) {
            this._done = true;
        }
        this._cursor = cursor;
    }
    get hasNext() {
        return !this._done;
    }
}
//# sourceMappingURL=cursor.js.map