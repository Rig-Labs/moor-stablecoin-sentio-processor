import { type WalletUnlocked } from '@fuel-ts/account';
import type { DeployedPredicate, FuelsConfig } from '../../types';
/**
 * Deploys one predicate.
 */
export declare function deployPredicate(wallet: WalletUnlocked, binaryPath: string, abiPath: string): Promise<{
    loaderBytecode: string;
    configurableOffsetDiff: number;
}>;
/**
 * Deploys all predicates.
 */
export declare function deployPredicates(config: FuelsConfig): Promise<DeployedPredicate[]>;
//# sourceMappingURL=deployPredicates.d.ts.map