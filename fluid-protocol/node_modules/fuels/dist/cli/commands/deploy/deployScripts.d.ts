import type { WalletUnlocked } from '@fuel-ts/account';
import type { FuelsConfig, DeployedScript } from '../../types';
/**
 * Deploys one script.
 */
export declare function deployScript(wallet: WalletUnlocked, binaryPath: string, abiPath: string): Promise<{
    blobId: string;
    loaderBytecode: string;
    configurableOffsetDiff: number;
}>;
/**
 * Deploys all scripts.
 */
export declare function deployScripts(config: FuelsConfig): Promise<DeployedScript[]>;
//# sourceMappingURL=deployScripts.d.ts.map